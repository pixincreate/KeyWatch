- id: keywatch-scan
  name: KeyWatch scan
  description: Scan staged changes for secrets, API keys, tokens, and credentials
  entry: key-watch scan --staged
  language: rust
  files: ''
  pass_filenames: false
  verbose: false
  args: ['--exit-mode=strict']

- id: keywatch-scan-system
  name: KeyWatch scan (system)
  description: Scan staged changes for secrets, API keys, tokens, and credentials
  entry: key-watch scan --staged
  language: system
  files: ''
  pass_filenames: false
  verbose: false
  args: ['--exit-mode=strict']
