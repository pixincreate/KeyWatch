[default]
extend-ignore-identifiers-re = [
    # HashiCorp is a real company name, not a typo for "Hash"
    "HashiCorp",
    # Fly.io token prefix pattern (fo1_)
    "fo1_",
]

[default.extend-words]
# "caf\xE9" (café as latin-1 bytes) in a non-UTF-8 test fixture
caf = "caf"
