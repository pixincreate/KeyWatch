# KeyWatch is a secret scanner: its test suite necessarily contains
# realistic fake credentials as detector fixtures. Exclude the test
# corpus from GitHub secret scanning and push protection so fixture
# changes are not blocked as leaked secrets. Everything outside
# tests/ remains scanned.
paths-ignore:
  - "tests/**"
